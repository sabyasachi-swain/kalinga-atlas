---
import Base from '@layouts/Base.astro';
import EntityCard from '@components/EntityCard.astro';
import WaveDivider from '@components/WaveDivider.astro';
import { getPorts, getPeriods, getSources } from '@data/load';
import { getEntry, render } from 'astro:content';
import { earliestPeriod, evidenceRank, sortChronological } from '@lib/sort-entities';

const periods = getPeriods();
// improvement-plan owner item 4: chronological is the server-rendered
// default (see @lib/sort-entities); the sort <select> below re-orders this
// same list client-side without a fresh page load.
const ports = sortChronological(getPorts(), periods);
const sources = getSources();
const periodsById = new Map(periods.map((p) => [p.id, p]));

const intro = await getEntry('sections', 'ports');
const { Content: IntroContent } = intro ? await render(intro) : { Content: undefined };
---

<Base
  title="Ancient Ports"
  description="Every port on the Kalinga coast documented on this atlas, with the evidence behind each one."
>
  <article class="section-page">
    <h1>Ancient Ports</h1>
    {
      IntroContent ? (
        <div class="intro">
          <IntroContent />
        </div>
      ) : (
        <p class="lede">
          The harbours and river landings that connected ancient Kalinga to the wider world.
        </p>
      )
    }

    <WaveDivider />

    <h2 class="visually-hidden">All ports</h2>
    {ports.length === 0 && <p class="empty">No published ports yet.</p>}
    {
      ports.length > 0 && (
        <form class="sort-form" data-sort-form>
          <label for="ports-sort">Sort by</label>
          <select id="ports-sort" name="sort">
            <option value="time" selected>
              Time (earliest first)
            </option>
            <option value="evidence">Evidence (strongest first)</option>
            <option value="az">A–Z</option>
          </select>
        </form>
      )
    }
    <ul class="cards" data-sort-list>
      {
        ports.map((p) => (
          <EntityCard
            kind="port"
            entity={p}
            sources={sources}
            sortTime={earliestPeriod(p, periodsById)?.order}
            sortEvidenceRank={evidenceRank(p.evidence_level)}
            earliestPeriodLabel={earliestPeriod(p, periodsById)?.label}
          />
        ))
      }
    </ul>
  </article>
</Base>

<script>
  import { initSortList } from '@lib/sort-list-client';
  initSortList();
</script>

<style>
  .section-page { padding: var(--space-8) var(--gutter); max-width: var(--content-max); margin: 0 auto; }
  .lede, .intro { font-size: var(--text-lg); color: var(--fg-muted); max-width: 46rem; }
  .empty { color: var(--fg-muted); font-style: italic; }
  .sort-form {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    margin: 0 0 var(--space-4);
    font-size: var(--text-sm);
  }
  .sort-form select {
    min-height: var(--hit-min);
    padding: var(--space-1) var(--space-2);
    border: 1px solid var(--rule);
    border-radius: var(--radius-sm);
    background: var(--bg-elevated);
    color: var(--fg);
    font: inherit;
  }
  .sort-form select:focus-visible { outline: none; box-shadow: var(--focus-ring); }
  .cards {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: var(--space-4);
    grid-template-columns: repeat(auto-fill, minmax(min(100%, 18rem), 1fr));
  }
</style>
